# Laissé vide : l'API est déduite automatiquement de l'hôte utilisé pour
# charger la page (voir src/api/client.ts). Ne définir VITE_API_URL que si
# l'API tourne sur un hôte différent du frontend (ex. déploiement séparé).
# VITE_API_URL=http://127.0.0.1:3000
